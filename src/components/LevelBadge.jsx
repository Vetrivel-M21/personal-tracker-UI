// Numeric level badge - display font, gradient background (primary -> secondary).
export default function LevelBadge({ level }) {
  return <span className="level-badge-display">Lvl {level}</span>;
}
